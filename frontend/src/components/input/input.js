import { React } from "react";
import "./input.css";

function Input(props) {
  return (
    <div class="Input">
      <input placeholder={props.name}></input>
    </div>
  );
}

export default Input;
s;
